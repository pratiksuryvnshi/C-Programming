#include <stdio.h>

void main()
{
	int x;
	printf("Enter the value = ");
	scanf("%d", &x);

	if (x > 5)
	{
		printf("Right Value is Greater than 5\n");
	}

	printf("God bless you\n");
}

// Pointer Type	 	Format Specifier	Typical Size (Bytes)
// char *			%p					4 or 8 bytes (depends on the system architecture)
// int *			%p					4 or 8 bytes (depends on the system architecture)
// short *			%p					4 or 8 bytes (depends on the system architecture)
// long *			%p					4 or 8 bytes (depends on the system architecture)
// long long *		%p					4 or 8 bytes (depends on the system architecture)
// float *			%p					4 or 8 bytes (depends on the system architecture)
// double *			%p					4 or 8 bytes (depends on the system architecture)
// void *			%p					4 or 8 bytes (depends on the system architecture)
